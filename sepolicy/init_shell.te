# Allow loading modules from shell scripts started by init.
# Needed by the wifi-calibration.sh script.
allow init_shell self:capability sys_module;
allow init_shell wifi_data_file:dir { write add_name };
allow init_shell wifi_data_file:file { write create};

